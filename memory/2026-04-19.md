# 2026-04-19 Session Log — MOLTmarket Phase 4.5 Live Testing

## Session Context
- **Goal:** Get Phase 4.5 (Money Reality Layer) running for live testing
- **User:** D.J. (Revenue's Mac mini)
- **Status:** Dashboard running, but MOLT feed + money-state endpoints not visible yet (routing issue)

## Work Completed This Session

### Phases Built (Summary)
1. **Phase 3.1 (Seed Agents)** — Agent Overfitter/Explorer/Risk Manager spawn real babies, MOLT persistence
2. **Phase 3.2 (MOLT Feed UI)** — Web interface for feed, filters, lineage detail, spawn button
3. **Phase 3.3 (Controlled Aliveness)** — Agent reaction loop (30-60s cadence), threading, personality-driven disagreement
4. **Phase 4 (Money Reality Layer)** — Hard risk controls (STOP/THROTTLE), promotion gates, capital allocator
5. **Phase 4.5 (Enforcement + Persistence)** — Execution enforcer, promotion enforcer, replacement logic, state persistence, money panel UI

### Key Architecture Decisions (Locked)
- **"New Run" Rule:** Clears metrics (PnL, trades, charts), NOT strategy state (active_bot, allocation, hypothesis links, mutation lineage)
- **SIM = LIVE:** Identical thresholds, identical enforcement, identical discipline
- **State Persistence:** Strategy state saved to `money_state.json`, survives session resets
- **Three Enforcement Layers:**
  1. Execution Enforcer — Every trade sized by allocation %
  2. Promotion Enforcer — ALL 7 gates must pass (no bypass)
  3. Replacement Logic — Auto-select best candidate when active bot dies

### Current Status
- Dashboard running on http://127.0.0.1:5050
- Arena executing (baseline bot trading, STOP triggered)
- MOLT UI layer initialized
- Routes registered: `/api/molt/feed`, `/api/molt/stats`, `/api/molt/lineage/<molt_id>`, `/api/molt/spawn/<molt_id>`
- **Issue:** `/api/money-state` not showing in routes list (decorator issue — function defined after `if __name__ == '__main__'` block)

### Bug Found + Fix
**Problem:** `/api/money-state` endpoint returning 404
**Root cause:** Function `api_money_state()` defined after Flask main block (line 1911 vs line 1779)
**Fix:** Moved function before main block so Flask sees the decorator
**Next:** Restart dashboard and verify endpoint appears in routes

## Testing Plan (Next Steps)
1. Restart dashboard after fix
2. Verify `/api/money-state` returns JSON
3. Test MOLT feed loading
4. Promote a bot manually → verify 1% allocation
5. Execute trade → verify size = base × 0.01
6. Hit STOP threshold → verify no new trades
7. Click "New Run" → verify metrics reset, strategy persists

## Key Files Created This Session
- `execution_enforcer.py` (5KB)
- `promotion_enforcer.py` (5.4KB)
- `replacement_logic.py` (9.5KB)
- `money_state_persistence.py` (6.7KB)
- `templates/money_panel.html` (11.3KB)
- `PHASE45-FINAL-LOCK-COMPLETE.md` (5.4KB)

## Critical Rule (Locked for Phase 4.5)
**"New Run resets metrics, NOT strategy state."**
This ensures users never accidentally lose their active bot, allocation, hypothesis links, or mutation lineage when clicking "New Run". Only the dashboard view clears.

## Next Actions
1. User restarts dashboard after code fix
2. Verify routes include `/api/money-state`
3. Test all 8 validation tests (trade sizing, STOP blocking, promotion gating, replacement logic, state persistence)
4. Report bugs here (not through THINK/MOLT)
5. Move to Phase 5 (multi-bot capital allocation) once all tests pass
